# n8n webhook endpoint for contact form submissions (server-side only, never exposed to browser)
N8N_WEBHOOK_URL=

# GitHub repo used to fetch Workdesk release downloads (format: owner/repo)
GITHUB_RELEASES_REPO=

# Bearer token sent in the Auth-Token header for webhook requests (server-side only)
WEBSITE_HEADER_TOKEN=
